# Task-Master UI (MVP)

This folder holds a minimal Next.js scaffold for the Task-Master UX MVP.

Quick start:

```bash
cd ui
npm install
npm run dev
```

The UI will connect to the Task-Master MCP server for data via API adapters.
