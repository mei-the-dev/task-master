export default function CalendarWidget({ events }) {
  return (
    <section>
      <h2>Calendar</h2>
      <div style={{ background: '#f9f9f9', border: '1px solid #ddd', minHeight: 200, padding: 16 }}>
        {/* Calendar grid and events would go here */}
        <em>[Calendar grid placeholder]</em>
      </div>
    </section>
  )
}
